# Тест-кейсы для API Avito QA-интерншип

## 📌 Общее замечание
API, согласно Postman-коллекции, реализует статистику по `adId` (идентификатору объявления).  
Однако в задании указано: **«получить статистику по itemID»** — т.е. по типу товара (`laptop`, `phone`).  
→ Это **расхождение между ТЗ и реализацией**.  
→ Мы тестируем **реальную реализацию**, но **зарегистрируем баг** в файле `BUGS.md`.

---

## 1. Создать объявление — POST /api/1/item

### TC-001: Успешное создание объявления
- **Цель**: Проверить, что объявление создаётся с корректными данными.
- **Действия**:
  1. Отправить POST-запрос на `/api/1/item` с телом:
     ```json
     {
       "sellerID": 123456,
       "name": "MacBook Pro 2023",
       "price": 150000,
       "statistics": {
         "likes": 5,
         "viewCount": 120,
         "contacts": 3
       }
     }
     ```
- **Ожидаемый результат**:
  - Статус: `200 OK`
  - В ответе есть поле `status` с текстом вида: `"Сохранили объявление - <UUID>"`
  - ID объявления извлекается из текста статуса

### TC-002: Создание с невалидным sellerID (меньше 111111) — БАГ В API
- **Цель**: Проверить, что API отклоняет sellerID вне диапазона.
- **Действия**:
  1. Отправить POST-запрос с `sellerID: 1`
- **Ожидаемый результат**:
  - Статус: `400 Bad Request`
  - Тело: `{"result": {"message": "sellerID must be between 111111 and 999999"}}`
- **Фактический результат**:
  - Статус: `200 OK`
  - Тело: `{"status": "Сохранили объявление - <UUID>"}` — объявление создано, хотя sellerID невалиден.
- **Вывод**: **БАГ** — API не валидирует `sellerID`, хотя это указано в ТЗ.



### TC-003: Создание без обязательного поля `name`
- **Цель**: Проверить валидацию обязательных полей
- **Действия**:
  1. Отправить POST-запрос без поля `name`
- **Ожидаемый результат**:
  - Статус: `400 Bad Request`
  - Ошибка: указывает на отсутствие `name`

---

## 2. Получить объявление по ID — GET /api/1/item/{adId}

### TC-004: Успешное получение объявления по существующему ID
- **Цель**: Проверить, что объявление возвращается корректно.
- **Действия**:
  1. Создать объявление (через TC-001) и сохранить его `id`
  2. Запросить GET `/api/1/item/{adId}`
- **Ожидаемый результат**:
  - Статус: `200 OK`
  - В ответе — массив с одним объектом, содержащим все данные (включая `createdAt`)

### TC-005: Получение объявления по несуществующему ID
- **Цель**: Проверить поведение при отсутствии объявления
- **Действия**:
  1. Запросить GET `/api/1/item/invalid-id-123`
- **Ожидаемый результат**:
  - Статус: `404 Not Found`
  - Тело: `{"result": "laborum", "status": "cillum enim eiusmod"}`

### TC-006: Получение с некорректным ID (строка вместо UUID)
- **Цель**: Проверить обработку невалидного формата ID
- **Действия**:
  1. Запросить GET `/api/1/item/abc123`
- **Ожидаемый результат**:
  - Статус: `400 Bad Request`
  - Тело: `{"result": {"messages": {...}, "message": "invalid adId format"}}`

---

## 3. Получить все объявления по sellerId — GET /api/1/{sellerId}/item

### TC-007: Успешное получение списка объявлений по sellerId
- **Цель**: Проверить, что возвращаются все объявления продавца
- **Действия**:
  1. Создать 2 объявления с одинаковым `sellerID: 777777`
  2. Запросить GET `/api/1/777777/item`
- **Ожидаемый результат**:
  - Статус: `200 OK`
  - В ответе — массив из 2 объектов, оба с `sellerId: 777777`

### TC-008: Получение по sellerId, у которого нет объявлений
- **Цель**: Проверить пустой ответ
- **Действия**:
  1. Запросить GET `/api/1/999999/item` (не существующий sellerId)
- **Ожидаемый результат**:
  - Статус: `200 OK`
  - Тело: `[]` (пустой массив)

### TC-009: Получение по sellerID вне диапазона (111111-999999)
- **Цель**: Проверить валидацию sellerID
- **Действия**:
  1. Запросить GET `/api/1/100000/item`
- **Ожидаемый результат**:
  - Статус: `400 Bad Request`
  - Ошибка: `sellerID must be between 111111 and 999999`

---

                  