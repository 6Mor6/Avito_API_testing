# Автоматизированные тесты для API Avito QA

## 🎯 Цель
Автоматизированный набор тестов для проверки API сервиса объявлений Avito QA-интерншип, реализованный с использованием **Python + pytest + requests**.

Тесты охватывают 3 ключевые ручки:
1. Создание объявления — `POST /api/1/item`
2. Получение объявления по ID — `GET /api/1/item/{adId}`
3. Получение всех объявлений по sellerId — `GET /api/1/{sellerId}/item`



---

## 📦 Технологии
- Python 3.8+
- `requests` — для HTTP-запросов
- `pytest` — для запуска тестов
- `python-dotenv` — для загрузки переменных окружения из `.env`

---

## 🛠️ Установка и запуск

1. **Склонируй репозиторий**:
   ```bash
   git clone https://github.com/ваш-пользователь/avito-api-testing.git
   cd avito-api-testing
    ```

2. Создай и активируй виртуальное окружение:
    ``` bash 
    python -m venv venv
    source venv/bin/activate     # Linux/macOS
    venv\Scripts\activate        # Windows

    ```

3. Установи зависимости:
    ``` bash
    pip install -r requirements.txt

    ```


4. Запусти все тесты:
    ```bash
    pytest tests/ -v
    ```

    * Для запуска отдельного теста:

        ```bash
        pytest tests/test_avito_api.py::test_create_ad_success -v    
        ```

## 📂 Структура проекта
avito-api-testing/

├── tests/ test_avito_api.py         
├── requirements.txt              
├── .env                          
├── README.md                     
├── TESTCASES.md                  
├── BUGS.md                       
└── .gitignore                    
        