## ID: 001

## Заголовок: API не валидирует sellerID — принимает значения вне диапазона 111111–999999

## Приоритет: Высокий  
## Серьёзность: Критическая

## 📌 Описание
API позволяет создавать объявления с `sellerID`, выходящим за допустимый диапазон (111111–999999), указанный в ТЗ.  
Это нарушает бизнес-правило уникальности и валидности продавца, что может привести к конфликтам данных между пользователями.

## 🔁 Шаги для воспроизведения
1. Отправить POST-запрос на `https://qa-internship.avito.com/api/1/item` с телом:
   ```json
   {
     "sellerID": 1,
     "name": "MacBook Pro 2023",
     "price": 150000,
     "statistics": {
       "likes": 5,
       "viewCount": 120,
       "contacts": 3
     }
   }

 ## Ожидаемый результат

 {
  "result": {
    "message": "sellerID must be between 111111 and 999999"
  }
}

## Фактический результат

{
  "status": "Сохранили объявление - f0c5a69c-b652-4916-8101-3c0a5c006acb"
}

## 🔗 Связано с
TESTCASES.md: TC-002
Автоматизированный тест: test_create_dont_valid

_______



## ID: 002

## Заголовок: API возвращает 400 вместо 404 при запросе несуществующего объявления

## Приоритет: Средний  
## Серьёзность: Средняя

## 📌 Описание
При запросе объявления по несуществующему ID (`/api/1/item/invalid-id-123`) API возвращает `400 Bad Request`,  
вместо ожидаемого `404 Not Found`, как указано в Postman-коллекции и соответствует стандартам REST.

## 🔁 Шаги воспроизведения
1. Отправить GET-запрос:  
   `GET https://qa-internship.avito.com/api/1/item/invalid-id-123`

## ✅ Ожидаемый результат (по спецификации)
- Статус: `404 Not Found`
- Тело: `{"result": "laborum", "status": "cillum enim eiusmod"}`

## ❌ Фактический результат
- Статус: `400 Bad Request`
- Тело: `{"result": {"message": "invalid adId format"}}`

## 🚨 Последствия
- Нарушает **REST-принципы**:  
  - `404` — ресурс не найден  
  - `400` — клиент отправил неверный запрос (ошибка в формате)  
- Клиенты не смогут корректно обрабатывать ошибки:  
  `400` — значит "ошибка в запросе" -> значит, **ID неверно сформирован**  
  `404` — значит "ID правильный, но объявления нет" -> **ожидаемое поведение**
- Затрудняет логику фронтенда, автоматизации и мониторинга.

## 💡 Рекомендация
Изменить поведение API:  
- При некорректном формате `adId` -> `400`  
- При корректном формате, но отсутствии объявления -> `404`  

Пример:  
- `GET /api/1/item/abc` → `400` (некорректный формат)  
- `GET /api/1/item/123e4567-e89b-12d3-a456-426614174000` (не существует) -> `404`

## 🔗 Связано с
- TESTCASES.md: TC-005  
- Автоматизированный тест: `test_get_dont_id`


______

## ID: 003

## Заголовок: Несогласованность в имени поля sellerId — создание vs получение

## Приоритет: Средний  
## Серьёзность: Средняя

## 📌 Описание
При создании объявления (POST /api/1/item) API ожидает поле `sellerID` (с большой `I`).  
При получении объявлений (GET /api/1/{sellerId}/item) API возвращает поле `sellerId` (с маленькой `i`).

Это **нарушает согласованность API** — клиенты не могут полагаться на единый формат.

## 🔁 Шаги воспроизведения
1. Создать объявление с `sellerID: 111119`
2. Получить объявления по `sellerId: 111119`
3. Увидеть, что в ответе поле называется `sellerId`

## ✅ Ожидаемый результат
- Поле должно называться **одинаково** при создании и получении:  
  либо всегда `sellerID`,  
  либо всегда `sellerId`.

## ❌ Фактический результат
- Создание: `sellerID`  
- Получение: `sellerId`

## 💡 Рекомендация
Выбрать **один стандарт** (рекомендуется `sellerId` — camelCase, как в REST):  
- Изменить **создание** — принимать `sellerId`  
- ИЛИ изменить **получение** — возвращать `sellerID`  
- **Задокументировать** выбор и **сделать релиз с backward compatibility**

## 🔗 Связано с
- TESTCASES.md: TC-001, TC-007  
- Автоматизированные тесты: `test_create_ad_success`, `test_get_list_seller`


## ID: 004

## Заголовок: API не валидирует sellerId — принимает значения вне диапазона 111111–999999 с кодом 200

## Приоритет: Высокий  
## Серьёзность: Высокий

## 📌 Описание
При запросе объявлений по `sellerId`, выходящему за допустимый диапазон (111111–999999), API возвращает `200 OK` с пустым списком `[]`,  
вместо ожидаемого `400 Bad Request`.

Это нарушает **бизнес-правило**:  
> *"Будьте аккуратны с sellerID — постарайтесь создать уникальный id (в диапазоне 111111-999999)"*  
— подразумевается, что **API должен отклонять** невалидные значения.

## 🔁 Шаги воспроизведения
1. Отправить GET-запрос:  
   `GET https://qa-internship.avito.com/api/1/99999994399/item`  
   (SellerId `99999994399` — явно выходит за диапазон 111111–999999)

## ✅ Ожидаемый результат
- Статус: `400 Bad Request`
- Тело:  
  ```json
  {
    "result": {
      "message": "sellerID must be between 111111 and 999999"
    }
  }

## ❌ Фактический результат
- Статус: 200 OK
- Тело: [] (пустой массив)  


## 🔗 Связано с

TESTCASES.md: TC-009
Автоматизированный тест: `test_get_dont_range`

